import React from 'react';
import PropTypes from 'prop-types';

const Index = props => {
    return (
        <div>
            Hello NextJS
        </div>
    );
};

Index.propTypes = {
    
};

export default Index;